import { Typography } from 'antd'
import React from 'react'

type Props = {}

const ListUser = (props: Props) => {
  return (
    <Typography.Title level={3}>ListUser</Typography.Title>

  )
}

export default ListUser